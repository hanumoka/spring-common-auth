spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        show_sql: true
        hbm2ddl:
          auto: none
        use_sql_comments: true
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
#    defer-datasource-initialization: on
  datasource:
    url: jdbc:mysql://localhost:3306/spring_common_auth?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
    hikari:
      username: root
      password: password

  redis:
    port: 6379
    host: 127.0.0.1
    jedis:
      pool:
        max-idle: 8
        min-idle: 0
        max-active: 8
  sql:
    init:
      mode: always
      data-locations: classpath:data.sql
      schema-locations: classpath:schema.sql

# springdocs
springdoc:
  swagger-ui:
    path: /api-docs/swagger
    tags-sorter: alpha
  api-docs:
    path: /api-docs/json
    groups:
      enabled: true
  cache:
    disabled: true
  default-consumes-media-type: application/json;charset=UTF-8
  default-produces-media-type: application/json;charset=UTF-8

custom:
  jwt:
    access-token:
      secret-key: lY9aYb+z5jR666e9x9W5oXOjqBa5eioaX1tqnMT60f4=
      expiration-milliseconds : 60000
    refresh-token:
      secret-key: ETgtmCXWisb5IW6enkMS0NfFmI3Yc7pJcaC2J3Me80I=
      expiration-milliseconds : 120000